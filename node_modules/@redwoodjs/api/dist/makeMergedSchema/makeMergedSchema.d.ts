import { IExecutableSchemaDefinition } from 'apollo-server-lambda';
import { GraphQLSchema } from 'graphql';
import { Services } from "../types";
/**
 * Merge GraphQL typeDefs and resolvers into a single schema.
 *
 * @example
 * ```js
 * const schemas = importAll('api', 'graphql')
 * const services = importAll('api', 'services')
 *
 * const schema = makeMergedSchema({
 *  schema,
 *  services: makeServices({ services }),
 * })
 * ```
 */
export declare const makeMergedSchema: ({ schemas, services, schemaDirectives, }: {
    schemas: {
        [key: string]: {
            schema: Record<string, unknown>;
            resolvers: Record<string, unknown>;
        };
    };
    services: Services;
    schemaDirectives?: IExecutableSchemaDefinition['schemaDirectives'];
}) => GraphQLSchema;
//# sourceMappingURL=makeMergedSchema.d.ts.map